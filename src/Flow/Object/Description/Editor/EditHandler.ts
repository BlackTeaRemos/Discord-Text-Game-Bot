/**
 * Legacy edit handlers removed.
 *
 * Edit functionality is now inline in DescriptionEditorFlow.__CreateEditCallback.
 * - HandleEditTrigger -> (inline in DescriptionEditorFlow)
 * - HandleModalSubmit -> (inline in DescriptionEditorFlow)
 * - DisableComponents -> Not needed, handled by Viewer timeout
 */
export {};
